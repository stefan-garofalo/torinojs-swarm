{
  "name": "@reaping/server",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./app": {
      "default": "./src/app.ts"
    }
  },
  "scripts": {
    "build": "rolldown -c",
    "check-types": "tsc -b",
    "deploy": "vercel",
    "deploy:prod": "vercel --prod",
    "lint": "oxlint",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs",
    "test": "bun test ./src/app.smoke.test.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.1",
    "@reaping/auth": "workspace:*",
    "@reaping/db": "workspace:*",
    "@reaping/ai": "workspace:*",
    "@reaping/env": "workspace:*",
    "effect": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "elysia": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@elysiajs/eden": "^1.4.8",
    "@reaping/config": "workspace:*",
    "@types/bun": "catalog:",
    "rolldown": "^1.0.0-rc.7",
    "typescript": "catalog:"
  }
}
