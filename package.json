{
  "name": "reaping",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "effect": "^3.19.19",
      "elysia": "^1.4.21",
      "zod": "^4.1.13",
      "typescript": "^5",
      "@types/bun": "^1.3.4",
      "better-auth": "1.5.2"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev:server": "turbo run dev --filter=@reaping/server",
    "dev:web": "turbo run dev --filter=@reaping/web",
    "build:server": "turbo run build --filter=@reaping/server",
    "build:web": "turbo run build --filter=@reaping/web",
    "check-types:server": "turbo run check-types --filter=@reaping/server",
    "check-types:web": "turbo run check-types --filter=@reaping/web",
    "lint": "turbo run lint",
    "lint:server": "turbo run lint --filter=@reaping/server",
    "lint:web": "turbo run lint --filter=@reaping/web",
    "test": "turbo run test",
    "test:server": "turbo run test --filter=@reaping/server",
    "db:push": "turbo run db:push --filter=@reaping/db",
    "db:studio": "turbo run db:studio --filter=@reaping/db",
    "db:generate": "turbo run db:generate --filter=@reaping/db",
    "db:migrate": "turbo run db:migrate --filter=@reaping/db",
    "check": "turbo run lint check-types"
  },
  "dependencies": {
    "@reaping/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@reaping/config": "workspace:*",
    "@types/bun": "catalog:",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0",
    "turbo": "^2.8.12",
    "typescript": "catalog:"
  },
  "packageManager": "bun@1.3.5"
}
